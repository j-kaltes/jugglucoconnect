services:
  rendezvous:
    build:
      context: .
    image: jugglucoconnect:local
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - "6789:6789/tcp"
    volumes:
      - type: bind
        source: ./fullchain.pem
        target: /data/fullchain.pem
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ./privkey.pem
        target: /data/privkey.pem
        read_only: true
        bind:
          create_host_path: false
